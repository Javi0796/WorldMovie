class MovieDetail {
    constructor(id, title, backdrop, page, descrip, runtime){
        this.id = id,
        this.title = title,
        this.backdrop = backdrop,
        this.page = page,
        this.descrip = descrip,
        this.runtime = runtime
    }
}

export default MovieDetail;