class Movie {
    constructor(genres, title, id, poster, date, descript){
        this.genres = genres,
        this.title = title,
        this.id = id,
        this.poster = poster,
        this.date = date,
        this.descript = descript
    }
}

export default Movie;