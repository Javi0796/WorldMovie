class Actor {
    constructor (id, gender, name, mainMovies, profile ) {
        this.id = id,
        this.gender = gender,
        this.name = name,
        this.mainMovies = mainMovies,
        this.profile = profile
    }
}

export default Actor;